
<div class="card" id="{{this._id}}">
    <div class="card-block">
        <h4 class="card-title">{{this.charity}}</h4>
        <p class="card-text">{{this.date}}</p>
        <p class="card-text">{{this.amount}}</p>
        <!-- Delete link -->
        <p><button class="btn btn-link" id="deleteComment" data-comment-id="{{this._id}}">Delete</button></p>
    </div>
</div>
